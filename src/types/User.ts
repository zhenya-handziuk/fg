export interface IUser {
    id: number;
    firstName: string;
    secondName: string;
    middleName: string;
    email: string;
    password: string;
    createdAt: string;
    updatedAt: string;
}
